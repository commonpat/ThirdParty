<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_RootView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:focusable="false">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:id="@+id/bg"
        android:visibility="gone"/>
    <include layout="@layout/mv_ablum_template_default" android:id="@+id/template_default" android:visibility="visible"/>
    <include layout="@layout/topic_default" android:id="@+id/template1" android:visibility="gone"/>
    <include android:id="@+id/topic_1" layout="@layout/topic_1" android:visibility="gone" />

</FrameLayout>