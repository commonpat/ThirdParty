<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/fl_song_list"
    android:layout_height="@dimen/dp80"
    android:background="@drawable/selector_play_list_song_item_bg"
    android:paddingTop="@dimen/dp15"
    android:paddingBottom="@dimen/dp15"
    >

    <ImageView style="@style/fl_song_list_play_icon" />

    <TextView
        android:id="@+id/tv_song_index"
        style="@style/fl_song_list_index"
        android:text="1" />

    <TextView
        android:id="@+id/tv_song_name"
        style="@style/song_list"
        android:layout_marginRight="@dimen/dp40"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="I  " />

    <TextView
        android:id="@+id/tv_free_tag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="免费"
        android:background="@drawable/shape_bg_free"
        android:textSize="@dimen/sp14"
        android:padding="@dimen/dp4"
        android:layout_gravity="right"
        android:layout_margin="@dimen/dp8"
        />

</FrameLayout>