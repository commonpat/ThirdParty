<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle" >

    <!-- 描边，边框 -->
    <stroke
        android:width="@dimen/dp2"
        android:color="@color/gray_red"/>

    <!-- 设置圆角半径 -->
    <corners android:radius="@dimen/dp30"/>

    <!-- 填充的颜色 -->
    <solid android:color="@color/thirdty_black" />

    <!-- 大小 -->
    <size
        android:width="@dimen/dp450"
        android:height="@dimen/dp250"/><!-- 宽度和高度 -->

</shape>